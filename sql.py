# import sqlite3
# conn = sqlite3.connect("test.db")
# # conn.execute("CREATE TABLE entries (id INTEGER PRIMARY KEY, category TEXT, value REAL)")
# conn.commit()
# print(conn.execute("SELECT * FROM entries").fetchall())

# def x (loch="ness"):
#     return(loch)

# print(x())

